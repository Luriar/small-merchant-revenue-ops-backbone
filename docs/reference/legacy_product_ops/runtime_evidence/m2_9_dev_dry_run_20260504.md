# M2-9 Dev Dry-Run Runtime Evidence (2026-05-04)

## Purpose

Sanitized timeline of the M2-9C controlled runtime dry-run executed on 2026-05-04 against the confirmed dev target `product-ops-dev-aurora`. This file is the destination referenced by `evidence_report_ref` across the M2-9 evidence set. It records the operator-supplied sanitized JSON output of `scripts/m2_9c_dry_run.js`.

## Run Context

- Target safe label: `product-ops-dev-aurora`
- Target environment: dev (non-production)
- Sample-count: 1
- Time-window: 10 minutes (watchdog enforced)
- Cleanup owner: Yoon Joonho
- Rollback owner: Yoon Joonho
- Execution path: repository-level controlled dry-run via `scripts/m2_9c_dry_run.js`, exercising the M2-8O Aurora repository through a thin transactional `pg.Client` adapter
- Connection method: operator's authorized dev path through a local SSM port-forward, with a dev-only SSL verification bypass scoped to this run
- Production-guard repair applied to `scripts/m2_9c_dry_run.js`: the original `currentDb.includes("prod")` substring rule false-positively rejected the dev DB `productops`. Replaced with explicit allowlist (`M2_9C_ALLOWED_DATABASE`, default `productops`) plus precise production-keyword detection (exact `prod`/`production`, suffixes `_prod`/`-prod`/`_production`/`-production`, substring `production`). Production safety is preserved.

## Sanitized Operator Stdout JSON

```json
{
  "target_safe_label": "product-ops-dev-aurora",
  "sample_count": 1,
  "time_window_minutes": 10,
  "evidence_report_ref": "docs/runtime_evidence/m2_9_dev_dry_run_20260504.md",
  "failure_id_pattern": "m2_9c_dryrun_<ts>_failure",
  "idempotency_key_pattern": "m2_9c_dryrun_<ts>_idem",
  "started_at_iso": "2026-05-04T11:32:19.402Z",
  "identity_safe_summary": {
    "db": "productops",
    "usr": "postgres",
    "sch": "public"
  },
  "pre_state_counts_for_synthetic_id": {
    "f": 0,
    "rr": 0,
    "sl": 0
  },
  "steps": {
    "seed_synthetic_failure": { "ok": true },
    "create_replay_request": {
      "ok": true,
      "observed_status": "requested",
      "observed_cleanup_status": "not_started",
      "replay_request_id_present": true,
      "response_field_count": 21
    },
    "idempotency_duplicate_lookup": {
      "ok": true,
      "same_replay_request_id": true
    },
    "idempotency_conflict_rejected": {
      "ok": true,
      "safe_class": {
        "mapped": "CdcRecoveryPersistenceError",
        "code": "internal_error",
        "statusCode": 500
      }
    },
    "state_log_appended": {
      "ok": true,
      "from_status": "open",
      "to_status": "replay_requested",
      "state_log_id_present": true
    },
    "valid_failure_transition": {
      "ok": true,
      "observed_status": "replay_requested"
    },
    "invalid_failure_transition_rejected": {
      "ok": true,
      "result_was_null": true
    },
    "valid_replay_request_transition": {
      "ok": true,
      "observed_status": "approved",
      "approved_at_present": true
    },
    "invalid_replay_request_transition_rejected": {
      "ok": true,
      "result_was_null": true
    }
  },
  "cleanup": {
    "failure_rows_deleted": 1,
    "post_state_counts_for_synthetic_id": {
      "f": 0,
      "rr": 0,
      "sl": 0
    },
    "cleanup_complete": true
  },
  "timing": {
    "elapsed_ms": 671,
    "within_bound": true
  },
  "completed_at_iso": "2026-05-04T11:32:20.073Z"
}
```

## Timeline (Derived From The JSON Above)

| Step | Outcome |
| --- | --- |
| Identity check (`current_database`, `current_user`, `current_schema`) | productops / postgres / public — passed |
| Pre-state row counts for synthetic `failure_id` | 0/0/0 — clean |
| Seed synthetic `cdc_failure` (status `open`) | ok |
| `createReplayRequest` | ok; `status=requested`, `cleanup_status=not_started`, `replay_request_id` present, 21 safe response fields |
| `findReplayRequestByIdempotencyKey` (duplicate lookup) | ok; same `replay_request_id` |
| Duplicate `createReplayRequest` (idempotency conflict) | rejected; `CdcRecoveryPersistenceError` (`code=internal_error`, `statusCode=500`) — safe repository-level rejection |
| `appendFailureStateLog` | ok; `from_status=open → to_status=replay_requested`; `state_log_id` present |
| `updateFailureStatus(open → replay_requested)` valid transition | ok; observed `status=replay_requested` |
| `updateFailureStatus(open → closed_no_replay)` invalid transition | rejected; UPDATE returned null (guard `from_status=open` no longer matched current `replay_requested`) |
| `updateReplayRequestStatus(requested → approved)` valid transition | ok; observed `status=approved`, `approved_at` set |
| `updateReplayRequestStatus(requested → cancelled)` invalid transition | rejected; UPDATE returned null |
| Cleanup `DELETE FROM public.cdc_failure WHERE failure_id = $1` | 1 row deleted; CASCADE removed dependent rows |
| Post-cleanup row counts for synthetic `failure_id` | 0/0/0 — cleanup complete |
| Total elapsed | 671 ms; within 10-minute bound |

## Repository-Level Rejection Note

The idempotency-conflict rejection was observed at the repository persistence boundary, not at the route layer. The `uq_cdc_replay_idempotency_key` unique-constraint violation in PostgreSQL was wrapped by the M2-8O Aurora repository as `CdcRecoveryPersistenceError` (`code=internal_error`, `statusCode=500`) — the safe error class the service layer catches and re-maps. The route-level 409 `idempotency_conflict` envelope mapping was **not** live-tested in M2-9C; that mapping is covered by the route-level tests with the stub repository under M2-8B and M2-8I.

## Sanitization Note

This evidence file deliberately omits:

- DB URLs, connection strings, JDBC URLs
- hostnames, IP addresses, ports, port-forward target names
- IAM role ARNs, AWS account identifiers
- credentials, tokens, passwords, secret references
- raw payload values, full message bodies, issue raw values, prod_change payload or actor values
- stack traces, SQL error internals (no `SQLSTATE` literal, no `detail`, `hint`, `position`, or `internalQuery` fields recorded)
- persistence internals beyond the safe error class shape

## Cross-References

- M2-9C evidence: `docs/m2_9c_controlled_runtime_dry_run_evidence_kr.md`
- M2-9C decision record: `docs/m2_9c_runtime_decision_record_kr.md`
- M2-9C cleanup report: `docs/m2_9c_runtime_cleanup_report_kr.md`
- M2-9C feasibility check: `docs/m2_9c_runtime_feasibility_check_kr.md`
- M2-9C synthetic input plan: `docs/m2_9c_synthetic_input_plan_kr.md`
- Operator dry-run script: `scripts/m2_9c_dry_run.js`
- M2-9B SQL apply evidence: `docs/m2_9b_sql_apply_evidence_kr.md`
- M2-9A GO master: `docs/m2_9a_live_db_preflight_go_evidence_kr.md`
